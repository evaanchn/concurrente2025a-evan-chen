procedure main()
  input const skater_count, const room_capacity
  shared can_skate := create_semaphore(room_capacity)
  join_threads(create_threads(skater_count, skater))
end procedure

procedure skater()
  wait(can_skate)
    // Concurrency-bounded region
    skate()
  signal(can_skate)
end procedure
